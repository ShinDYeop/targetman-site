# 보안 운영 규칙 — 타겟맨 신동엽

## 1. 비밀값은 저장소에 넣지 않습니다

API 키, 비밀번호, 토큰은 **코드나 설정 파일에 절대 쓰지 않습니다.** 배포 환경변수로만 관리하며,
소스에서는 서버 전용 코드에서 `env`를 통해서만 읽습니다.

현재 사용하는 환경변수

| 이름 | 용도 | 노출 범위 |
|---|---|---|
| `ADMIN_PASSWORD` | 상담 접수함(`/admin`) 비밀번호 | 서버 전용 |
| `OWNER_EMAIL` | 새 상담 알림을 받을 주소 | 서버 전용 |
| `RESEND_API_KEY` | 알림 메일 발송 키 (설정 전에는 알림이 조용히 꺼집니다) | 서버 전용 |

**하지 말아야 할 것**

- `.env` 파일을 만들어 커밋하기
- 키를 소스에 하드코딩하고 "나중에 빼기"
- 키를 채팅, 이슈, 스크린샷에 붙여넣기
- 클라이언트 코드(`app/src/routes`, `app/src/components`)에서 키를 참조하기

키는 `*.server.ts` 파일과 `createServerFn` 핸들러 안에서만 읽습니다. 이 규칙을 지키면
빌드 과정에서 키가 브라우저 번들에 섞여 들어갈 수 없습니다.

## 2. 저장소에 키가 들어갔는지 확인하는 법

```bash
git grep -nE "re_[A-Za-z0-9]{12,}|sk-[A-Za-z0-9]{16,}|AKIA[0-9A-Z]{16}|ghp_[A-Za-z0-9]{20,}"
```

한 줄도 나오지 않아야 정상입니다. 만약 이미 커밋된 키를 발견했다면 **파일을 지우는 것으로는
부족합니다.** 히스토리에 남아 있으므로 순서는 이렇습니다.

1. 해당 키를 발급처에서 즉시 폐기(revoke)
2. 새 키 발급
3. 새 키를 환경변수로 등록
4. 그다음에 코드 정리

## 3. 적용된 방어 장치

| 위협 | 대응 |
|---|---|
| 관리자 비밀번호 무차별 대입 | SHA-256 해시 비교(길이도 노출되지 않음), 최근 실패 횟수에 비례한 응답 지연, 15분 내 30회 실패 시 잠금 |
| 접수 폼 스팸/봇 | 허니팟 필드, 최소 작성 시간 2.5초, 번호당 시간당 3건, 전체 시간당 60건 상한 |
| 개인정보 장기 보관 | 접수가 있을 때마다 180일 지난 기록 자동 삭제. 방어용 로그는 24시간 |
| 접수함 캐시/색인 유출 | `/admin`에 `no-store`, `X-Robots-Tag: noindex`, `robots.txt` 차단 |
| 입력값 오염 | 제어문자·비가시문자 제거, 길이 제한, 전화번호 형식 검증 |
| MIME 스니핑, 리퍼러 유출, 불필요한 권한 | `X-Content-Type-Options`, `Referrer-Policy`, `Permissions-Policy` 적용 |
| SQL 인젝션 | 모든 쿼리를 바인딩 파라미터로 작성. 문자열 결합 금지 |
| XSS | React가 기본 이스케이프. `dangerouslySetInnerHTML` 사용 금지 |

## 4. 의도적으로 넣지 않은 것과 이유

- **HSTS** — 상위 도메인을 다른 사이트와 공유하고 있어 `includeSubDomains` 영향이 큽니다.
  자체 도메인으로 옮긴 뒤에 켜는 것이 맞습니다.
- **frame-ancestors / X-Frame-Options** — 이 플랫폼의 디자인 편집기가 배포된 사이트를
  iframe으로 엽니다. 막으면 편집 기능이 깨집니다. 자체 호스팅으로 옮기면 바로 켜야 합니다.
- **IP 기준 속도 제한** — 현재 런타임에서 클라이언트 IP를 신뢰할 수 있게 얻기 어려워,
  번호 단위 빈도와 봇 신호로 대체했습니다.

## 5. 운영 수칙

- `/admin` 비밀번호는 카톡이나 메모 앱에 평문으로 남기지 마세요.
- 접수함 화면에는 고객 이름과 연락처가 있습니다. 공용 PC, 화면 공유, 촬영 중에는 열지 마세요.
- 고객이 개인정보 삭제를 요청하면 즉시 지워야 합니다. 요청 창구를 페이지에 적어 두었습니다.
- 알림 메일에는 고객 연락처가 포함됩니다. 메일함 비밀번호와 2단계 인증을 점검하세요.
